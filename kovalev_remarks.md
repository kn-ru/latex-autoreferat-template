# Замечания С. П. Ковалева по диссертации и автореферату

Файл консолидирует все замечания Ковалева в одном месте. Не для устранения «прямо сейчас» - просто учет.

---

## Раунд 1. Общая претензия и три базовых принципа

**Главная претензия:** «Ваша диссертация выглядит как отличный отчет инженера-разработчика, а должна читаться как труд ученого-исследователя».

Три базовых принципа, требующих исправления:

1. **Глобальная абстракция (смена оптики).** Не «научил нейросеть лучше искать людей по камерам», а «решил фундаментальную математическую задачу распознавания сложных многокомпонентных структур по их разрозненным частям». Человек (с его лицом и силуэтом) - удобный, сложный частный случай для тестирования теории.

2. **Сначала формулы, потом люди (изменение структуры).** Сперва описать универсальный методический аппарат (трансформеры, ArcFace, байесовская модель, MAP-критерий) в математическом вакууме. Только потом показывать, как эта математика решает прикладную задачу поиска людей.

3. **Доказательство универсальности (масштабируемость).** Если метод действительно научный и фундаментальный, он не может работать только на людях. Подсветить опубликованные работы автора про дефекты металла, номера машин и БПЛА. Они доказывают, что предложенная архитектура распознает что угодно, если разбить объект на части.

**Сопутствующие технические уточнения (из ответного письма автора):**

- Привести описание байесовской интеграции в строгое соответствие с программной реализацией (свидетельство о регистрации № 2026617996 от 23.03.2026). **Удалить избыточную формулу с весовым коэффициентом α** и явно описать реальный механизм автоматического взвешивания модальностей через ковариационные матрицы класса $\Sigma_y$ в гауссовых правдоподобиях. Дополнительный гиперпараметр комбинирования не нужен.
- Добавить в третью главу раздел «Масштабируемость метода» со ссылками на работы по коррозии, номерам и БПЛА.
- Добавить характеристики серверной инфраструктуры экспериментов.
- Восстановить определения переменных в формулах.
- Обосновать выбор архитектуры YOLOv8 (блоки C2f, SPPF).
- Упомянуть Акт о внедрении в ФГБУ «НИЦ «Институт имени Н. Е. Жуковского»» (сокращение времени принятия решения до 5 % в системах визуального наблюдения).
- Исправить оформительские пометки в списке публикаций.
- Унифицировать терминологию («многокомпонентная структура»), оформление по ГОСТ.

### Статус по раунду 1 (на 2026-04-27)

В автореферате (`autoreferat.md`) и во введении диссертации (`Dissertation/introduction.tex`) переработаны: актуальность, объект, предмет, цель, задачи, новизна, теор/практ значимость, положения на защиту, краткое содержание, заключение - под фундаментальную постановку «многокомпонентные структуры», ReID - частный случай. Добавлены два новых блока во введении (формальная постановка задачи и универсальный методический инструментарий). Положение 3 на защиту приведено к формулировке через $\Sigma_{y,k}$, без гиперпараметра комбинирования. Расширены ссылки на работы автора в смежных предметных областях.

---

## Раунд 2. Замечания после переработки введения

Получены по итогам чтения переработанного автореферата. Введение действительно переработано (сформулированы общие методы распознавания многокомпонентных структур и выделен частный случай распознавания людей), однако:

1. **Основное наполнение работы осталось смесью общих и частных положений.** Тезис о том, что метод «успешно масштабируется на другие предметные области», в основном тексте не подтвержден.

2. **Поправка к функции потерь YOLOv8 имеет члены с индексом «face».** Формула с $\mathcal{L}_{\text{face}}$ и $\lambda_{\text{face}}$ остается специфичной для распознавания лиц; для соответствия абстрактной постановке индексы должны быть обобщены (например, $\mathcal{L}_{\text{inner}}$ / $\lambda_{\text{inner}}$ - «вложенный компонент»), а интерпретация «inner = face» дана отдельно как частный случай.

3. **Все схемы процессов и алгоритмов содержат специфику процедуры распознавания людей.** Блок-схемы методики (рис. 1, рис. 3 автореферата и аналогичные в диссертации) изображают конкретный конвейер «силуэт-лицо», без обобщенного варианта для произвольного $K$ и произвольных компонентов структуры.

4. **Байесовская модель также специфична.** В тексте главы 2 модель описана для пары $(\mathbf{f}, \mathbf{s})$ (лицо, силуэт); абстрактный многокомпонентный случай $\{\mathbf{z}_k\colon k\in\mathcal{M}(t)\}$ во введении дан, но в основном тексте не раскрыт.

5. **Пункт 3 научной новизны - «разработан абстрактный метод интеграции...» - не раскрыт в главе 2.** Глава 2 содержит описание метода для двух конкретных модальностей, абстрактный метод как таковой в ней не сформулирован.

6. **Название работы.** «Комплексная методика реидентификации людей в информационно-поисковых системах» расходится с продекларированной абстрактной постановкой. Вероятно, требуется переформулировка - например, «Методика распознавания многокомпонентных структур в информационно-поисковых системах (на примере реидентификации людей)» или аналогичная конструкция с явным указанием частного случая в подзаголовке.

7. **Частный вопрос по терминологии.** «Множество идентичностей» (используется в тексте автореферата и во введении диссертации) звучит неестественно и режет ухо. Возможные замены: «множество личностей», «множество субъектов», «множество классов идентификации», «множество идентификаторов объекта» - в зависимости от контекста.

### Статус по раунду 2 (на 2026-05-21)

Все замечания устранены в ветке `2.3.8` (коммиты `cc86518`, `6f6dd4e` и последующие коммиты переработки под стратегию Б):

- **Раунд 2, п. 1** (масштабируемость подтверждена): добавлен раздел `\section{Масштабируемость метода на смежные предметные области}` в `Dissertation/part3.tex` со ссылками на работы автора по коррозии, номерам, БПЛА, антиспуфингу и космическим аппаратам.
- **Раунд 2, п. 2** (индекс «face» → «inner»): глобально заменены `\lambda_{\text{face}}` → `\lambda_{\text{inner}}`, `\mathcal{L}_{\text{face}}` → `\mathcal{L}_{\text{inner}}`, `\mathbb{I}_{\text{face}}` → `\mathbb{I}_{\text{inner}}` во введении, в гл. 2 и 3 диссертации, в `Synopsis/data.tex` и `Synopsis/content.tex`, в `common/concl.tex`. В алгоритме `BboxLoss` переименованы переменные `lambda_face/loss_face/face_iou/face_mask` → `..._inner`. Конкретные обозначения `\Phi_{\text{face}}`, `I_{\text{face}}`, `ROI_{face}` оставлены там, где речь идет именно о применении к лицу как одной из двух модальностей.
- **Раунд 2, п. 3** (схемы): закрыт коммитом `017a239`. Помимо подписей существующих рисунков, к ним добавлены параллельные обобщенные TikZ-схемы (`Synopsis/images/scheme-ipc-generic.tikz` и `Synopsis/images/scheme-bayes-generic.tikz`, дублируются в `Dissertation/images/` для диссера): обобщенный конвейер ИПС для произвольного числа компонентов структуры $K$ и обобщенная блок-схема байесовской интеграции с MAP-критерием. Существующие растровые рисунки `images/1.png` и `images/3.png` сохранены и явно отмечены как «Частный случай ($K=2$): силуэт-лицо».
- **Раунд 2, п. 4-5** (байесовская модель и пункт 3 новизны в главе 2): в подраздел `\subsection{Байесовская модель интеграции}` (`Dissertation/part2.tex`) добавлен параграф «Общий случай: произвольное число компонентов структуры» с формулами для $\mathcal{M}(t)\subseteq\{1,\ldots,K\}$, многоклассового MAP-критерия по~произвольному набору компонентов и многомерных гауссиан $P(\mathbf{z}_k\mid y)$ с матрицами $\Sigma_{y,k}$. Далее идет параграф «Частный случай: $K=2$, реидентификация людей по паре «силуэт-лицо»», в котором происходит конкретизация на пару $(\mathbf{f},\mathbf{s})$.
- **Раунд 2, п. 6** (название): **название приведено к формулировке Акта о внедрении** - «Методика распознавания и реидентификации людей в~информационно-поисковых системах» (ранее «Комплексная методика реидентификации людей...»). Полная переформулировка к абстрактной форме «распознавания многокомпонентных структур» не выполнена, так как название фиксируется Актом о внедрении в~ФГБУ «НИЦ «Институт имени Н. Е. Жуковского»» и оформлено на формулировку с «людьми». Абстрактная рамка при этом развернута во введении и в~автореферате (ReID как наиболее представительный частный случай), в~главе~2 (общий случай произвольного~$K$) и~в~гл.~3 (раздел «Масштабируемость метода на~смежные предметные области»).
- **Раунд 2, п. 7** («множество идентичностей»): термин не используется; везде применяется «множество классов идентификации».

## Раунд 3. Замечания после чтения переработки под стратегию Б

Получены в ходе итоговой доработки (2026). Дословный текст:

> Переход от описания решения конкретной прикладной задачи к описанию
> абстрактной научной проблемы и подхода к ее решению на иллюстративном
> примере по-прежнему выполнен очень фрагментарно. В частности, п.2 научной
> новизны, положений на защиту и заключения следует переформулировать
> соответственно. А в основном тексте диссертации всюду, кроме п.1.2 (0,5
> стр.), п.2.2.3 (2 стр.) и п.3.4 (2 стр.), говорится исключительно о
> распознавании людей. Необходима переработка - тренируйте навык абстрактного
> научного мышления! И в п.3.4 нельзя ограничиваться только общими
> рассуждениями - нужно привести численные значения показателей качества
> распознавания многокомпонентных структур в нескольких смежных задачах. Ну и
> в табл.4 автореферата неясно, почему на разных наборах данных использовались
> разные методы для сравнения с предложенным. Также в диссертации необходимо
> привести ссылки на литературу по каждому из методов, указанных в первой
> колонке этой таблицы.

Разбивка на пункты и статус (на 2026-06-07):

1. **Абстракция фрагментарна; п.2 новизны/положений/заключения переформулировать; основной текст переработать.** **Выполнено** (по гл. 1 - в основном). П.2 и остальные пункты научной новизны, положений на защиту и заключения переписаны «абстрактным-первым»: ведущая сущность - распознавание многокомпонентных структур по их разрозненным частям, реидентификация людей (силуэт-лицо, $K=2$) - наиболее представительный частный случай и апробация (`Dissertation/introduction.tex`, `common/concl.tex`, `Synopsis/data.tex`, `Synopsis/content.tex`). Основной текст глав 2 и 3 переведен на сквозную рамку многокомпонентных структур (компоненты структуры, внешний/вложенный компонент, общий случай $K$, оговорки «в рассматриваемом случае»), конкретика ReID вынесена как частный случай. В главе 1 рамка задана в названии, постановке задачи для произвольного $K$, обобщенной схеме информационных процессов и выводах; обзорный п. 1.1 (конкретные методы распознавания лиц: PCA, LDA, Виола-Джонс, HOG) по своей природе предметно-специфичен.

2. **В п.3.4 привести численные значения качества распознавания в смежных задачах.** **Выполнено** (в честном объеме). В раздел 3.4 добавлены численные показатели для двух смежных задач, реально устроенных покомпонентно: распознавание регистрационного знака как структуры из символов (детектирование знака mAP[0,5:0,95] 81,2 %, детектирование символов 95,2 %, распознавание символов 99,32 % - `Rusakov2020license`, табл. `tab:ch3/scal-lp`) и мультикамерная оценка движения БПЛА как интеграция частичных наблюдений (`Rusakov2023uavspeed`; приведенное число характеризует точность оценивания кинематики, а не классификацию). Коррозия, антиспуфинг и прогноз состояния КА оставлены как качественные свидетельства широты без приписывания им предложенного метода; отдельный эксперимент под смежную задачу не ставился (по решению автора).

3. **В табл. 4 неясно, почему на разных наборах сравнивались разные методы.** **Выполнено.** В диссертацию (`Dissertation/part3.tex`, после таблицы `tab:ch3/sota`) и в автореферат добавлено пояснение: большинство методов отчитывается лишь на одном-двух наборах из CUHK03/Market-1501/MARS (прочерк - отсутствие публикации, а не низкое качество); наборы относятся к покадровому (CUHK03, Market-1501) и видеорежиму (MARS); значения CUHK03 получены при разных протоколах (ранний single-shot 1367/100 против современного 767/700, labeled/detected); предложенный метод оценен в закрытой Top-1, большинство сравниваемых - в открытом retrieval. Для сопоставимости добавлены сквозные анкеры MGN и BDB, отчитывающиеся одновременно на CUHK03 (767/700, labeled) и Market-1501.

4. **Привести ссылки на литературу по каждому методу первой колонки табл. 4.** **Выполнено.** К каждой строке-методу таблицы `tab:ch3/sota` в `Dissertation/part3.tex` проставлен `\cite`; в `biblio/external.bib` добавлен 21 источник (20 SOTA-методов и комбинация B-BOT+OSM+CL Centers), все верифицированы по первоисточникам (arXiv/CVF/AAAI/IEEE/ACM). Диссертация собирается без неразрешенных ссылок.

### Контекст стратегии Б

В ветке `2.3.8` принято решение о реализации **стратегии Б** (распознавание многокомпонентных структур, ReID как наиболее представительный частный случай). Промежуточный поворот в стратегию А (коммиты `b33d300`, `2d95bff`) был связан с миграцией под специальность 1.2.1 (КФУ) и зафиксирован в ветке `main`. Для защиты по специальности 2.3.8 в ИПУ РАН замечания раундов 1 и 2 учтены целиком, раунд 3 - по всем четырем пунктам (по п. 2 - в честном объеме: численные показатели добавлены для двух реально декомпозиционных смежных задач).

---

## Источники

- `email_kovalev.md` - ответное письмо автора с описанием уже выполненных доработок (соответствует раунду 1).
- Раунд 2 - текст сообщения Ковалева, полученного 2026-04-27.
- Раунд 3 - текст сообщения Ковалева, полученного в ходе итоговой доработки (2026); дословно приведен выше.
