%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%% Дополнительные настройки шаблона                             %%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%% Настройки отображения подписей на титульном листе %%%

% Показывать ли подпись диссертанта на автореферате
\newcounter{showperssign}
\setcounter{showperssign}{0}  % 0 - не показывать, 1 - показывать

% Показывать ли подпись ученого секретаря на автореферате
\newcounter{showsecrsign}
\setcounter{showsecrsign}{0}  % 0 - не показывать, 1 - показывать

% Показывать ли информацию об оппонентах и ведущей организации
\newcounter{showopplead}
\setcounter{showopplead}{1}   % 0 - не показывать, 1 - показывать

%%% Дополнительные команды %%%

% Команда для создания пустой линии заданной ширины
\newcommand{\blank}[1]{\rule{#1}{0.4pt}}

% Команда для тире
\renewcommand{\cyrdash}{\mbox{---}}

% Команда для центрирования широких объектов (уже есть в memoir)
% \centerfloat уже определена в классе memoir, поэтому не переопределяем
